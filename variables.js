//defining variables

// ; seperates codes, defines where the line ends
var a = 100 ; //with numbers calculations can be made
var b = 'dogukan' ; // quotes makes the variable readable (no calc)

a = a - 10; // calculations with variables now a = 90
a = a*3 // a = 270

//defining booleans (they are either false or true)

var c = false;

//undefined variables
var d; //Javascript reads it as undefined

//rather than having var keyword const(constant) and let() is used
//const cannot be modified and redecleared
//with var variables can be changed and redecleared
//let values can be changed

console.log('Hey Dogukan');
console.log(a);
console.log(b);
console.log(d);